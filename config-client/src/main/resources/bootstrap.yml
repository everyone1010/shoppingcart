spring:
  profiles: 
    active: development 
  application:
    name: shoppingcart
  cloud:
    config: 
      uri: http://localhost:8981

management:
  security:
    enabled: false
          
redishost: ${message}