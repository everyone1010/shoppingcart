project: 
  name: zipkinserver
  version: 1.0
  description: User MicroServices using Spring Boot
  artifact: com.msn.poc.tracing
info:
  build:
    artifact: ${project.artifactId}
    name: ${project.name}
    description: ${project.description}
    version: ${project.version}

management:
  security:
    enabled: false


spring:
  application:
    name: ${project.name}
  cloud:
    config:
      enabled: true